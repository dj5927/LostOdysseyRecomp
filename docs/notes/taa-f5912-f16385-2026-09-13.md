# TAA jitter mapping for captures f5912 and f16385 — 2026-09-13

This note records local Vulkan temporal-jitter candidates from two follow-up captures. It does not establish a release result or whole-game coverage.

The user accepted the reported f5912 and f16385 flicker scenes after the latest candidate was delivered. This acceptance covers those reported scenes only; other scenes, whole-game behavior and release status remain unverified.

## Captured paths

Capture f5912 added these observed vertex-shader identities:

| Vertex-shader identity | Jitter mapping |
| --- | --- |
| `52e4405f97159d2f` | `c4` |
| `97b5d441419b5533` | `c7` |
| `799c02c8b6582bfe` | `c8` |
| `eeae6424413228d6` | `c8` |

The related draws were 1, 1 and 10 across three frames, for 12 draws total. The existing `f5997-fix.exe` candidate was used with a 1080p configuration, while the captured temporal scene was 3840×2160; the camera changed in the third frame.

Capture f16385 added two observed identities:

| Vertex-shader identity | Jitter mapping |
| --- | --- |
| `fe3efe042c311110` | `c4` alpha-test depth |
| `1474db97dfc0afad` | `c7` light |

It contained four draws per frame, for 12 draws total. The reviewed VS paths have independent position and UV/lighting chains, so no pixel-shader compensation was added.

The paired f16385 evidence matched draw 197 to 869 and draw 200 to 870. Their world `c0`–`c3`, two `vfetch95` words, index base and index count were identical. The depth `tex0` and light `tex2` fetch words matched across all six fetches, including the shared alpha texture; light `tex0` was a `0x17000` white texture.

## Audit and fixture evidence

For both captures, the three-frame `audit.json` records passed source byte-FNV identity checks and matching actual-use slots and camera state. The f5912 fixture passed on its first run: 787,980 checks across 32 phases at 1080p and 4K, with a maximum legacy separation of `0.487849` pixels. Its result is recorded in `out/flicker-20260913-f5912/fixture-result.log`.

The f16385 fixture also passed on its first run: 787,980 checks across 32 phases at 1080p and 4K, with a maximum legacy separation of `0.487722` pixels. Each capture ran only its new fixture; earlier fixture results were reused. The combined Release build completed successfully without rebuilding PPC. Evidence records are [f5912 audit.json](../../out/flicker-20260913-f5912/audit.json), [f5912 audit.py](../../out/flicker-20260913-f5912/audit.py), [f5912 build.log](../../out/flicker-20260913-f5912/build.log), [f16385 audit.json](../../out/flicker-20260913-f16385/audit.json), [f16385 audit.py](../../out/flicker-20260913-f16385/audit.py) and [f16385 build.log](../../out/flicker-20260913-f16385/build.log).

These are bounded source, audit and CPU-fixture results. User acceptance applies to the reported f5912 and f16385 scenes; no GPU replay, other-scene coverage or whole-game conclusion is recorded. Raw private capture exports remain outside the repository.

## Candidate delivery

The source-0.5.9 development candidate `LostOdysseyRecomp-f16385-fix.exe` includes the f5997, f5912 and f16385 mappings. It is 83,453,952 bytes with SHA-256 `892674b3c3272216d97d90cf29807e8cf20f25ff0bf604d380c713d3efae03f3`, matching its build identity record. It was copied beside the existing executable without launching it. The original executable was preserved. See `out/flicker-20260913-f16385/delivery.json`. No commit, push or release was performed.
