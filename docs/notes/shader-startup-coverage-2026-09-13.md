# Startup shader coverage follow-up — 2026-09-13

The local change moves the newly observed `1474db97dfc0afad` declaration variant into the existing startup shader preparation path. Its original source, `11bc08f69da45bb3`, was already extracted from original game resources; the missing coverage was CPX SDK metadata plus the packed 40-byte static-mesh declaration. The earlier log recorded a 32.178 ms DXC compile for this variant during gameplay.

The maintainer generator now accepts SHA-256 and size-verified decoded CPX package inventories alongside the original raw FPD inventory. It emits metadata for 245 vertex sources, nine declarations, 130 pixel representatives and 2,512 bounded link plans. Existing raw-package pixel representatives retain priority, so equivalent CPX discoveries do not add new source dependencies to old plans. The generated tables contain identities and patch metadata, not game microcode. The packed declaration comes from this capture and original SDK semantics; it is not claimed to be a recovered constant XEX constructor.

Startup bundle snapshots now include a generated SHA-256 identity of both metadata tables. Changed discovery coverage therefore causes source expansion to run again. Individual compiled-shader identities and the translator version remain unchanged, allowing valid compiled binaries to be reused. First preparation may take longer as new candidates are compiled. This changes shader preparation coverage; it does not move all pipeline creation or resource loading out of gameplay.

Validation uses isolated original resource inputs. The CPX regression extracts `11bc08f69da45bb3` from the original decoded container, generates variants before opening the captured reference, and reproduces all 564 bytes of `1474db97dfc0afad`. Input preservation and corrupted-source rejection passed. The historical 354 fixed outputs remain byte-identical. Generator guards reject an altered decoded-package digest, failed decoder inventory and invalid container offset.

The preceding capture-specific flicker repairs were accepted by the user. This startup coverage change remains local and requires gameplay frame-time validation; it is not a claim that every reported frame drop is resolved. Fourteen additional TAA family candidates remain static analysis candidates and are not enabled by this change. At this validation checkpoint, no commit, push or release was recorded. The subsequent user request assigns these source changes to `0.5.10` for commit and push; the tested candidate below retains its original `0.5.9` build identity.

Local diagnostic evidence is retained in `out/shader-startup-research-20260913/`, including `REPORT.md`, `packed40-metadata.json` and focused fixture logs. Original assets and captures are not included in repository documentation.

The final linked regression used 245 VS and 130 PS extracted from original SDK containers, produced 6,936 linked outputs and preserved all 2,245 historical combined references byte-for-byte. PS source gating, cancellation and save-failure propagation passed. `LoShaderStartupCacheTest` passed discovery identity invalidation before any consumer callback, together with its existing bundle-integrity and rollback checks. Its missing `fmt::fmt` link dependency was corrected. These were focused CPU/native tests, with no game launch or repeated TAA tests.

The Release runtime build passed and the candidate was copied to `D:\Mihoyo\LostOdysseyRecomp-windows-x64\LostOdysseyRecomp-precompile-fix.exe`, with its matching `.exe.build.json`. Source version is `0.5.9` at dirty `eaaa8a9fda21dd89d7dad9bcfe059964637cdc2f`; binary size is 83,534,848 bytes and SHA-256 is `9fd97d256ae0f7979d8bbc50184b38e14f9de0eab4c1c9a2dbd523a3cd220976`. The existing baseline and prior fix candidates were preserved. The final metadata determinism check also passed after stabilizing pixel representative priority.
