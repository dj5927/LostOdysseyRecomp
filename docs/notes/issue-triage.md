# Automated issue triage

**Status: deployment in progress from `main` (2026-09-14).** The repository secret `ISSUE_TRIAGE_API_KEY` was created on 2026-09-14. The workflow and its first CI run are pending this push; successful activation is not yet claimed.

The repository contains a bounded, non-agentic workflow for preliminary analysis of newly opened GitHub Issues. `.github/workflows/issue-triage.yml` runs on `issues.opened` and also supports manual `workflow_dispatch` with the required `issue_number` input and a `dry_run` input that defaults to `true`. The job runs on GitHub-hosted Ubuntu and invokes `tools/issue_triage/triage.py`; it does not execute issue text, model output, commands or attachments.

The model gateway is configured through the repository secret `ISSUE_TRIAGE_API_KEY`. Repository variables can override `ISSUE_TRIAGE_BASE_URL` and `ISSUE_TRIAGE_MODEL`; the defaults are `https://api.zkx.ca/v1` and `gpt-5.6-luna`. The implementation uses the `chat/completions` endpoint. It limits repository context and model output, rejects redirects and non-HTTPS base URLs, screens the generated comment for credentials and `@` mentions, and uses the marker `<!-- lost-odyssey-issue-triage:v1 -->` to avoid duplicate bot comments. Manual dry runs write a local `issue-triage-preview.md` preview and do not post to GitHub.

The implementation is now staged for push from the latest `main` worktree. After the push, the maintainer should verify that the workflow appears on the default branch and that CI can read `ISSUE_TRIAGE_API_KEY`; optional repository variables `ISSUE_TRIAGE_BASE_URL` and `ISSUE_TRIAGE_MODEL` can override the documented defaults. The API key used for local testing is now stored as the repository secret with maintainer approval; it is not recorded in source or documentation. The recorded HTTP checks reached `/models` successfully and received a successful `chat/completions` response. The worker's 11 mock checks passed, covering `finish_reason: stop`, strict dry-run behavior, POST id/body verification and duplicate prevention. A real Issue #27 dry run generated a Spanish preview without posting to GitHub; the saved preview is `out/issue-triage/issue-27-preview.md`. These checks do not establish deployed workflow execution or issue-author acceptance.

简体中文：这是新 Issue 的自动初步分析回复工具，当前已在最新 `main` worktree 中准备推送；secret 已于 2026-09-14 创建，待本次 push 和 CI 核验后确认启用。可选的 repository variables 为 `ISSUE_TRIAGE_BASE_URL` 和 `ISSUE_TRIAGE_MODEL`。它不代表问题已复现、已修复或已获得报告者验收；修复工作仍需另行确认。
