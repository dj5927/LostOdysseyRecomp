# Automated issue triage

**Status: enabled on both `main` mirrors (2026-09-14).** The implementation was published at `dce8608`; the encoding diagnostic correction is included at `a116d1c`. The repository secret `ISSUE_TRIAGE_API_KEY` was recreated without a BOM after an initial PowerShell BOM prevented encoding the authentication header.

The repository contains a bounded, non-agentic workflow for preliminary analysis of newly opened GitHub Issues. `.github/workflows/issue-triage.yml` runs on `issues.opened` and also supports manual `workflow_dispatch` with the required `issue_number` input and a `dry_run` input that defaults to `true`. The job runs on GitHub-hosted Ubuntu and invokes `tools/issue_triage/triage.py`; it does not execute issue text, model output, commands or attachments.

The model gateway is configured through the repository secret `ISSUE_TRIAGE_API_KEY`. Repository variables can override `ISSUE_TRIAGE_BASE_URL` and `ISSUE_TRIAGE_MODEL`; the defaults are `https://api.zkx.ca/v1` and `gpt-5.6-luna`. The implementation uses the `chat/completions` endpoint. It limits repository context and model output, rejects redirects and non-HTTPS base URLs, screens the generated comment for credentials and `@` mentions, and uses the marker `<!-- lost-odyssey-issue-triage:v1 -->` to avoid duplicate bot comments. Manual dry runs write a local `issue-triage-preview.md` preview and do not post to GitHub.

The workflow is enabled for `issues.opened`; optional repository variables `ISSUE_TRIAGE_BASE_URL` and `ISSUE_TRIAGE_MODEL` can override the documented defaults. Hosted workflow run [34886197607](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34886197607) succeeded, and GitHub workflow ID `358120201` is active. The API key is stored as the repository secret and is not recorded in source or documentation. The recorded HTTP checks reached `/models` successfully and received a successful `chat/completions` response. The worker's 11 mock checks passed, covering `finish_reason: stop`, strict dry-run behavior, POST id/body verification and duplicate prevention; one additional encoding-sanitizer regression also passed. A real Issue #27 dry run generated a preview without posting to GitHub, and its downloaded artifact was reviewed successfully. The first live comment on a newly opened Issue has not yet been observed. These checks do not establish issue-author acceptance.

简体中文：这是已在双端 `main` 启用的新 Issue 自动初步分析回复工具；GitHub hosted run `34886197607` 已成功，workflow ID `358120201` 当前为 active，真实 #27 dry run artifact 已下载审阅且未 POST。首次真实新 Issue 评论尚未观察；它不代表问题已复现、已修复或已获得报告者验收，修复工作仍需另行确认。
