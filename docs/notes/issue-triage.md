# Automated issue triage

**Status: deployed on both `main` mirrors (2026-09-14).** The `@codex` comment and code-retrieval update was published at `600b08e`. A real public `@codex` reply remains to be observed after a user submits one.

The repository contains a bounded, non-agentic workflow for preliminary analysis of newly opened GitHub Issues. `.github/workflows/issue-triage.yml` runs on `issues.opened` and also supports manual `workflow_dispatch` with the required `issue_number` input, a `dry_run` input that defaults to `true`, and an optional `comment_id` for previewing an existing mention. The workflow also handles a new human `@codex` request in an Issue comment when the author is an OWNER, MEMBER or COLLABORATOR; pull requests are excluded, while a closed Issue may still receive a manually requested analysis. Each new qualifying mention is analyzed by comment ID, so an earlier automated triage marker does not block a later request. The job runs on GitHub-hosted Ubuntu and invokes `tools/issue_triage/triage.py`; it does not execute issue text, model output, commands or attachments.

To request a follow-up analysis, an authorized collaborator can comment, for example: `@codex 请结合代码库分析这个问题，列出相关文件、可能原因和下一步验证。` The workflow reads the issue, recent human discussion (up to 12 comments), and ranked first-party Git-tracked code snippets (up to 6 snippets and 24,000 characters) for read-only model context. It replies as `github-actions[bot]`; this is a repository workflow and does not use the official Codex App comment identity.

The model gateway is configured through the repository secret `ISSUE_TRIAGE_API_KEY`. Repository variables can override `ISSUE_TRIAGE_BASE_URL` and `ISSUE_TRIAGE_MODEL`; the defaults are `https://api.zkx.ca/v1` and `gpt-5.6-luna`. The implementation uses the `chat/completions` endpoint. It limits repository context and model output, rejects redirects and non-HTTPS base URLs, screens the generated comment for credentials and `@` mentions, and uses the marker `<!-- lost-odyssey-issue-triage:v1 -->` to avoid duplicate bot comments. Manual dry runs write a local `issue-triage-preview.md` preview and do not post to GitHub.

The repository secret `ISSUE_TRIAGE_API_KEY` and optional variables `ISSUE_TRIAGE_BASE_URL` and `ISSUE_TRIAGE_MODEL` configure the deployed workflow. The earlier hosted run [34886197607](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34886197607) succeeded, and the updated manual dry-run [34887742345](https://github.com/freefrank/LostOdysseyRecomp/actions/runs/34887742345) also succeeded. Because that dispatch omitted `comment_id`, it verified the regular issue-routing path and does not establish a real `issue_comment` event. The earlier 11 mock checks and encoding-sanitizer regression passed; the updated suite adds 7 retrieval tests, for 19 triage/mention plus 7 retrieval checks passed. A local synthetic mention using a real Issue #27, source context and the API produced a Chinese preview; it explicitly reported insufficient code evidence and did not post. Snippet sorting is complete. These checks do not establish issue-author acceptance; the first public `@codex` reply remains pending user observation.

简体中文：`@codex` 评论入口和代码检索更新已随 `600b08e` 在双端 `main` 发布。19 项 triage/mention 与 7 项 retrieval 检查已通过；workflow_dispatch dry run `34887742345` 成功，但未传 `comment_id`，因此不等同于真实 `issue_comment` 事件验证。本地使用真实 Issue #27、源码和 API 的中文预演成功，明确代码证据不足且未 POST。首次公开 `@codex` 回复仍待用户实际使用观察，修复工作仍需另行确认。
