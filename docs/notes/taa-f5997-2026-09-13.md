# TAA jitter mapping for capture f5997 — 2026-09-13

This note records a local Vulkan temporal-jitter candidate for the f5997 capture. It does not establish a release result or whole-game coverage.

The user accepted the reported f5997 flicker scene after the candidate was delivered. This acceptance covers the reported scene only; other scenes, whole-game behavior and release status remain unverified.

## Captured scene

The capture covers frames 5997–5999 from source version 0.5.9 on Vulkan with an RTX 5080, 3840×2160 output and AA3. The camera was stationary. Each frame contained nine relevant draws, for 27 draws total, with guest viewport `1280×720`, `VTE=0x43f` and `RB_DEPTH_INFO=0x10000`. Known depth passes were jittered; the five material/light passes recorded here were previously unrecognized.

## Mapping and shader review

The local `temporal_scene.h` mapping covers the five observed vertex-shader identities as follows:

| Vertex-shader identity | Jitter constant bank |
| --- | --- |
| `5f0bd44481510be0` | `c7` |
| `310850b1446f0e41` | `c7` |
| `e242d31a3f1acdc4` | `c7` |
| `5d98f5e3bcc3f4ae` | `c8` |
| `a9dd56801863b1f0` | `c8` |

The reviewed VS paths send the transformed position to position/clip output. Their lighting and UV calculations are independent of the camera transform, so the candidate does not add pixel-shader reconstruction compensation. For the reviewed PS inputs, `tex0` screen-buffer use was observed at `b0d9000` for VS `a9dd56801863b1f0` draw 948 and VS `310850b1446f0e41` draw 1021, with producer resolve draw 947; `e242` sampled a 1×1 white texture. These observations guide the bounded mapping and do not prove PS execution behavior.

## Fixture evidence

`tools/tests/temporal_jitter_test.cpp` with `tools/tests/f5997_jitter_capture.h` passed `--captured-f5997-layers` on the first attempt: 590,985 checks across 32 jitter phases at 720p and 4K, covering nine captured draws and five shader identities. The fixture covers nine captured constant banks, preserves Z/W and non-position constants, keeps paired PS constants unchanged, rejects camera/depth mismatches without writes, and reports a maximum legacy separation of `0.487662` pixels. The recorded result is in `out/flicker-20260913-f5997/fixture-result.log`.

## Build and candidate delivery

The source version 0.5.9 Release build completed with exit code 0 with an incremental build. No PPC rebuild was performed. The parallel candidate `LostOdysseyRecomp-f5997-fix.exe` is 83,453,952 bytes with SHA-256 `fe7ee4852219570a873906a8af13cd61101f7b7e57d6da3e0cc0df4756eb7bc3`; its `.exe.build.json` matched. The primary baseline remained available with SHA-256 `ef93c01db40433fb6d463357ea3e6979b3a1f45cf0d2d3f81be2fcf3f8885faa`, and the candidate was not launched. The delivery and audit records are [delivery.json](../../out/flicker-20260913-f5997/delivery.json), [audit.json](../../out/flicker-20260913-f5997/audit.json), [audit.py](../../out/flicker-20260913-f5997/audit.py) and [build.log](../../out/flicker-20260913-f5997/build.log). The audit covered all three captured frames, nine draws per frame, matching camera state and all five microcode byte-FNV identities.

This is a CPU fixture over captured constants and independently transcribed shader chains. Three baseline screenshots were captured for the scene, but no before/after screenshot comparison of a repaired build, GPU replay, cross-scene coverage or player visual acceptance was performed. The raw private capture exports and source manifests remain outside the repository; only the bounded fixture and audit records are referenced here.
